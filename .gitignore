.DS_Store
.vercel
node_modules/
*.log
.env
.env.local

# Local-only credentials (never check in)
.pangram_key
*.local-secret
metadata.local.json

# Local-only screenshot outputs (Playwright skill)
screenshots/
