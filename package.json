{
  "name": "cezarbabin",
  "version": "1.0.0",
  "private": true,
  "description": "Static blog at cezarbabin.com. The only Node code is the /api/og Edge function that generates per-article share images.",
  "dependencies": {
    "@vercel/og": "^0.6.5"
  }
}
